name: Ceevee theme companion plugin
version: 0.0.1
description: Extends the config/site panel

dependencies:
    - { name: grav, version: '>=1.2.4' }
    - { name: form, version: '>=2.4.0' }
    - { name: admin, version: '>=1.4.2' }

